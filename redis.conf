# Redis configuration file
# Disable stop-writes-on-bgsave-error to prevent Redis from refusing writes
# when RDB snapshotting fails (common in Docker environments)

# Disable the stop-writes-on-bgsave-error option
stop-writes-on-bgsave-error no

# Other recommended settings for Docker environments
save ""
appendonly no
maxmemory-policy noeviction
